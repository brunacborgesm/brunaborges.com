# Sections

Sections are the big building blocks of a page. 

Each section (like "Hero", "About", or "Contact") is a self-contained component. This makes it super easy to swap things around or update a specific part of the page without breaking the rest.
